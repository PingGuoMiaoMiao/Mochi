收到。我们可以按你列出的顺序推进。建议把工作拆成以下三个阶段，每阶段都形成可运行/可测试的增量：
阶段 1：语法补齐（优先清除未用 Token）
Struct 初始化表达式
AST：补充 ExprKind::StructInit（记录类型名、字段列表、范围）。
词法：若需要 ::、=、? 等附加符号的特殊处理，先确认规范，目前 ColonColon 等已定义。
解析：
Ident 后跟 TokenKind::LBrace → 解析字段 name: expr，支持尾逗号；
考虑 Struct { ..base } 等扩展，如果规范要求。
测试：添加白盒用例覆盖正常初始化、缺冒号、缺右括等错误。
switch 语句 & match 模式解构
AST：
新增 StmtKind::Switch、Pattern 枚举（Wildcard、Literal、Ident、Tuple、Struct 等），PatternArm 结构。
词法：确认 ::、=>、|（模式 OR）、as 等关键字/符号是否已支持。
解析：
switch (expr) { case pattern => stmt; … default => stmt }；
match 分支中解析 Pattern，支持 _、字面量、结构解构（Point { x, y }）、元组、pattern if guard 等规范项；
将 OperatorSlash、OperatorEq、OperatorSlashEq 等剩余 Token 在模式或表达式里用上（例如 /= 属于复合赋值，可在赋值实现中补齐）。
测试：构造多层嵌套 match/switch、缺 =>、缺 case、非法模式等快照。
逻辑赋值与剩余 Token
完成 &&=、||=、??=（如文法存在）；
确认 /=, %= 等复合赋值运算选择性支持；
根据规范引入 OperatorSlash（除法）在 parse_factor 中已经使用？ yes we check factor uses /; so warning maybe because not emitted? oh parser uses OperatorSlash for division; but enumeration warns variant never constructed: due to lexer not emitting? maybe not used? Wait TokenKind::OperatorSlash; we never emit? in lexer star eq etc but for / we apparently missing. Need add case '/' with eq? to produce OperatorSlash etc.
阶段 2：测试矩阵扩展
解析：
结构体初始化成功/失败；
switch & match 模式各分支；
深度后缀链 + match 嵌套；
复合错误（缺 ;、缺 }、非法模式等）验证 ParseError。
词法：
复合赋值、=>、::、注释/换行等组合；
错误输入（非法字符、未闭合结构）判断 LexError 收集。
阶段 3：类型检查 / IR / LLVM 设计草案
定义 Type, TypeEnv, SymbolTable, TyckError；
设计 Analyzer.analyze(program : Array[Stmt]) -> TyckResult 接口；
规划 KNF/IR 数据结构与 lower_ir 函数签名；
梳理 MoonLLVM 使用方式，草拟 emit_llvm 管线；
同步将当前 AST/Token 暴露的必要信息整理为公共接口。